#include "holberton.h"
#include <stdio.h>
/**
 * int_isupper - uppercase character
 * @c: variable
 *
 * Return: if c is uppercase 1 or 0 other wise
 */
int _isupper(int c);
{
int a = c;
if (a >= 65 && a <= 90)
return (1);
else
return (0);
}
